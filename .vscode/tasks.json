{
	"version": "2.0.0",
	"tasks": [
	  {
		"label": "Build expressionRepl",
		"type": "shell",
		"command": "cmake --build --preset build-debug",
		"group": {
		  "kind": "build",
		  "isDefault": true
		},
		"problemMatcher": ["$gcc"],
		"detail": "Build the project using CMake presets."
	  }
	]
  }
